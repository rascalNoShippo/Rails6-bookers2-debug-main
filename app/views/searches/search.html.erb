<div class='container px-5 px-sm-0'>
  <div class='row'>
    <!--div class='col-md-3'>
      <h2>Results index</h2>
      <%#= render 'info', user: @user %>
      <h2 class="mt-3">New book</h2>
      <%#= render 'books/form', book: @book %>
    </div-->
    <div class='col-md-8 offset-md-1'>
      <h2>Results index</h2>
      <table class="table table-hover table-inverse">
        <tbody>
          <% @lists.each do |item| %>
            <tr>
              <td>
                <%= link_to "#{params[:model].pluralize.downcase}/#{item.id}" do %>
                  <div class="d-flex row">
                    <div class="col-3"><%= item.send(@col_1) %></div>
                    <div class="col-9"><%= item.send(@col_2) %></div>
                  </div>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>